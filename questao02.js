//2 - Crie um algoritmo que converte dias em horas, quando recebe um número de dias.


function converter(dias){
    const horas = dias * 24
    return ` A quantidade em horas é: ${horas}`;
}
console.log(converter(2))
